version: 2

before:
  hooks:
    - go mod tidy
    - go vet ./...

builds:
  - id: richmond
    main: .
    binary: richmond
    ldflags:
      - -s -w
      - -X github.com/misfitdev/richmond/cmd.version={{.Version}}
      - -X github.com/misfitdev/richmond/cmd.commit={{.ShortCommit}}
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64

archives:
  - id: default
    formats: [tar.gz]
    name_template: "richmond_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

dockers_v2:
  - id: richmond
    images:
      - "ghcr.io/misfitdev/richmond"
    tags:
      - "{{ .Version }}"
      - latest
    dockerfile: Dockerfile.goreleaser
    platforms:
      - linux/amd64
      - linux/arm64
    sbom: false
    labels:
      org.opencontainers.image.title: richmond
      org.opencontainers.image.description: Sync Google Workspace users and groups to any SCIM v2 endpoint
      org.opencontainers.image.url: https://github.com/misfitdev/richmond
      org.opencontainers.image.source: https://github.com/misfitdev/richmond
      org.opencontainers.image.version: "{{ .Version }}"
      org.opencontainers.image.licenses: MIT

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - "^docs:"
      - "^chore:"
      - "^ci:"
  groups:
    - title: Features
      regexp: "^.*(?:Add|add|feat).*$"
      order: 0
    - title: Bug Fixes
      regexp: "^.*(?:Fix|fix|Handle|handle).*$"
      order: 1
    - title: Other
      order: 999

release:
  github:
    owner: misfitdev
    name: richmond
  draft: false
  prerelease: auto
  name_template: "Richmond {{ .Version }}"
